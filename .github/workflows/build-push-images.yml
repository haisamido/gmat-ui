# Build and publish GMAT Docker images to GitHub Container Registry
#
# Builds three targets from Containerfile:
#   wasm       - Browser-based GMAT (node:20-slim, port 8989)
#   native     - Console GMAT (ubuntu:26.04)
#   native-gui - X11 GUI GMAT (ubuntu:26.04)
#
# Images are pushed as:
#   ghcr.io/haisamido/gmat-ui/gmat-{web,console,x11}:<branch>  (always)
#   ghcr.io/haisamido/gmat-ui/gmat-{web,console,x11}:latest    (main branch only)
#
# Usage:
#   docker pull ghcr.io/haisamido/gmat-ui/gmat-web:latest
#   docker run -p 8989:8989 ghcr.io/haisamido/gmat-ui/gmat-web:latest
#   docker run -it ghcr.io/haisamido/gmat-ui/gmat-console:latest
#   docker run -it ghcr.io/haisamido/gmat-ui/gmat-x11:latest
#
# To enable arm64: uncomment the arm64 matrix entries below and the
# manifest job, then switch the build job to push-by-digest mode.
# Native arm64 runners (ubuntu-24.04-arm) avoid slow QEMU emulation.

name: Docker Image CI

on:
  push:
    branches: ["main", "dev", "issue-*"]
  pull_request:
    branches: ["main", "dev"]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: wasm
            image_name: gmat-web
          - target: native
            image_name: gmat-console
          - target: native-gui
            image_name: gmat-x11
          # --- arm64 (uncomment to enable multi-arch) ---
          # - target: wasm
          #   image_name: gmat-web
          #   runner: ubuntu-24.04-arm
          #   platform: linux/arm64
          # - target: native
          #   image_name: gmat-console
          #   runner: ubuntu-24.04-arm
          #   platform: linux/arm64
          # - target: native-gui
          #   image_name: gmat-x11
          #   runner: ubuntu-24.04-arm
          #   platform: linux/arm64

    steps:
      - name: Free up disk space
        if: ${{ !env.ACT }}
        run: rm -rf /opt/hostedtoolcache

      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        if: github.event_name != 'pull_request'
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository }}/${{ matrix.image_name }}
          tags: |
            type=raw,value=${{ github.ref_name }}
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}

      # Push builds: tag, push to GHCR
      - name: Build and push
        if: github.event_name != 'pull_request'
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Containerfile
          target: ${{ matrix.target }}
          push: true
          platforms: linux/amd64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            GIT_COMMIT_FULL=${{ github.sha }}
            REPO_COMMIT_URL=https://github.com/${{ github.repository }}/commit/${{ github.sha }}
          cache-from: type=gha,scope=${{ matrix.target }}
          cache-to: type=gha,scope=${{ matrix.target }},mode=max

      # PR / local builds: validate only, no login or push needed
      - name: Build (validation)
        if: github.event_name == 'pull_request'
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Containerfile
          target: ${{ matrix.target }}
          push: false
          load: true
          platforms: linux/amd64
          provenance: false
          build-args: |
            GIT_COMMIT_FULL=${{ github.sha }}
            REPO_COMMIT_URL=https://github.com/${{ github.repository }}/commit/${{ github.sha }}

  # --- Multi-arch manifest (uncomment when arm64 builds are enabled) ---
  # manifest:
  #   if: github.event_name != 'pull_request'
  #   needs: build
  #   runs-on: ubuntu-latest
  #   permissions:
  #     packages: write
  #   strategy:
  #     fail-fast: false
  #     matrix:
  #       include:
  #         - image_name: gmat-web
  #         - image_name: gmat-console
  #         - image_name: gmat-x11
  #   steps:
  #     - name: Download digests
  #       uses: actions/download-artifact@v4
  #       with:
  #         path: /tmp/digests
  #         pattern: digest-${{ matrix.image_name }}-*
  #         merge-multiple: true
  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3
  #     - name: Log in to GitHub Container Registry
  #       uses: docker/login-action@v3
  #       with:
  #         registry: ${{ env.REGISTRY }}
  #         username: ${{ github.actor }}
  #         password: ${{ secrets.GITHUB_TOKEN }}
  #     - name: Create manifest list and push
  #       working-directory: /tmp/digests
  #       run: |
  #         tags=("-t" "${{ env.REGISTRY }}/${{ github.repository }}/${{ matrix.image_name }}:${{ github.ref_name }}")
  #         if [ "${{ github.ref }}" = "refs/heads/main" ]; then
  #           tags+=("-t" "${{ env.REGISTRY }}/${{ github.repository }}/${{ matrix.image_name }}:latest")
  #         fi
  #         digests=$(printf "${{ env.REGISTRY }}/${{ github.repository }}/${{ matrix.image_name }}@sha256:%s " *)
  #         docker buildx imagetools create "${tags[@]}" $digests
